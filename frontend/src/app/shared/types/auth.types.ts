/**
 * Re-exports from AuthService — canonical types live in auth.service.ts.
 * Import directly from '@core/services/auth.service' instead.
 */
export type { User, TokenResponse, SignInRequest, SignUpRequest } from '../../core/services/auth.service';
