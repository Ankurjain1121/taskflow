// Re-export from canonical shared location for backward compatibility
export type {
  FocusTask,
  ProjectPulse,
  StreakData,
  SnoozedTaskEntry,
  SnoozedTasksMap,
} from '../../shared/types/dashboard.types';
